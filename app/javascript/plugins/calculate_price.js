const calculatePrice = () => {
  // Recuperer tous les inputs
  const year = document.getElementById("booking_starting_date_1i").value;
  const month = document.getElementById("booking_starting_date_2i").value;
  const day = document.getElementById("booking_starting_date_3i").value;
  // On recupere le prix par jour


  // On ecoute le change des inputs de dates
    // Parse les dates de debut et de fin
    // On calcule le nombre de jours
    // On calcule le prix total
    // On l'insere dans le champ Price
}

export default calculatePrice;
